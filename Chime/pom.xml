<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <!--
    POM generated by gwt-maven-plugin archetype
  -->
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.paxxis</groupId>
  <artifactId>Chime</artifactId>
  <packaging>war</packaging>
  <version>1.0-SNAPSHOT</version>

  <properties>

      <!-- convenience to define GWT version in one place -->
  	<gwt.version>2.0.4</gwt.version>

  	<!--  tell the compiler we can use 1.5 -->
      <maven.compiler.source>1.5</maven.compiler.source>
      <maven.compiler.target>1.5</maven.compiler.target>

  </properties>

  <dependencies>

    <dependency>
      <groupId>com.google.gwt</groupId>
      <artifactId>gwt-servlet</artifactId>
      <version>${gwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.google.gwt</groupId>
      <artifactId>gwt-user</artifactId>
      <version>${gwt.version}</version>
    </dependency>
	<dependency>
		<groupId>com.paxxis</groupId>
		<artifactId>ChimeServiceClient</artifactId>
		<version>1.0-SNAPSHOT</version>
	</dependency>
	<dependency>
	  <groupId>googlecode-paxxis</groupId>
	  <artifactId>gwt-visualization</artifactId>
	  <version>1.x-PAXXIS</version>
	</dependency>
    <dependency>
      <groupId>sencha-paxxis</groupId>
      <artifactId>gxt</artifactId>
      <version>2.1.4</version>
    </dependency>
    <dependency>
      <groupId>sencha-paxxis</groupId>
      <artifactId>gxt-servlet</artifactId>
      <version>2.1.4</version>
    </dependency>
	<dependency>
		<groupId>commons-fileupload</groupId>
		<artifactId>commons-fileupload</artifactId>
		<version>1.2.2</version>
	</dependency>
	<dependency>
		<groupId>googlecode-paxxis</groupId>
		<artifactId>gwteventservice</artifactId>
		<version>1.2.0-PAXXIS</version>
	</dependency>
	<dependency>
		<groupId>googlecode-paxxis</groupId>
		<artifactId>eventservice</artifactId>
		<version>1.2.0-PAXXIS</version>
	</dependency>
	<dependency>
		<groupId>googlecode-paxxis</groupId>
		<artifactId>eventservice-rpc</artifactId>
		<version>1.2.0-PAXXIS</version>
	</dependency>
	<dependency>
	  <groupId>eu.medsea.mimeutil</groupId>
	  <artifactId>mime-util</artifactId>
	  <version>2.1</version>
	</dependency>

    <!-- test -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.7</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
    <distributionManagement>
        <repository>
            <uniqueVersion>true</uniqueVersion>
            <id>paxxis-contrib</id>
            <name>Releases</name>
            <url>http://repo.paxxis.com/content/repositories/releases</url>
            <layout>default</layout>
        </repository>
        <snapshotRepository>
            <uniqueVersion>true</uniqueVersion>
            <id>paxxis-contrib-snapshot</id>
            <name>Snapshots</name>
            <url>http://repo.paxxis.com/content/repositories/snapshots</url>
            <layout>default</layout>
        </snapshotRepository>
    </distributionManagement>

  <build>
    <outputDirectory>src/main/webapp/WEB-INF/classes</outputDirectory>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>gwt-maven-plugin</artifactId>
        <version>1.3.1.google</version>
        <executions>
          <execution>
            <goals>
              <goal>compile</goal>
              <goal>test</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          	<runTarget>chime/Chime.html</runTarget>
        </configuration>
      </plugin>
      <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-war-plugin</artifactId>
          <configuration>
            <webappDirectory>src/main/webapp</webappDirectory>
            <webXml>src/main/webapp/WEB-INF/web.xml</webXml>
            <warSourceExcludes>.gwt-tmp/**</warSourceExcludes>
			<failonmissingwebxml>true</failonmissingwebxml>
          </configuration>
      </plugin>
      <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>2.0.2</version>
          <configuration>
            <source>${maven.compiler.source}</source>
            <target>${maven.compiler.target}</target>
          </configuration>
      </plugin>
    </plugins>
  </build>
    <name>Chime Web App</name>
    <description>This is the Chime web application.</description>
</project>
